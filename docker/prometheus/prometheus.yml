global:
  scrape_interval:     15s
  evaluation_interval: 15s

alerting:
  alertmanagers:
    - static_configs:
        - targets:
          # - alertmanager:9093

rule_files:
# - "first_rules.yml"
# - "second_rules.yml"

scrape_configs:
  - job_name: 'transform'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['dataflow.localhost:9393']
    file_sd_configs:
      - files:
          - targets.json
        refresh_interval: 30s