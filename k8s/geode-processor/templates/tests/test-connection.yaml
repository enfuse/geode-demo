apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "geode-processor.fullname" . }}-test-connection"
  labels:
{{ include "geode-processor.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "geode-processor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
