apiVersion: v1
kind: Service
metadata:
  name: {{ include "geode-processor.fullname" . }}
  labels:
{{ include "geode-processor.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "geode-processor.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
